# Hint 1: Check the basics

Look for these common syntax errors:
- Dockerfile instructions should be UPPERCASE (FROM, not from)
- Each line in a multi-line RUN command needs a backslash (\) at the end
- Check for duplicate instructions that conflict