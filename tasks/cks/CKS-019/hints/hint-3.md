# CKS-019 Implementation Notes

This exercise is scaffolded. Build the broken setup state first, then replace the placeholder script check in `task.yaml` with executable validation for each listed criterion.
