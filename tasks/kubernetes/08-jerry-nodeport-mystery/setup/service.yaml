apiVersion: v1
kind: Service
metadata:
  name: jerry-nodeport
spec:
  type: NodePort
  selector:
    app: jerry-nodeport
  ports:
    - name: http
      port: 80
      targetPort: 8080
      nodePort: 30080
