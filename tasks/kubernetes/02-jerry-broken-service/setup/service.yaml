apiVersion: v1
kind: Service
metadata:
  name: jerry-app-service
spec:
  selector:
    app: jerry-app  # Wrong selector - doesn't match pod labels
    environment: production  # This label doesn't exist on pods
  ports:
  - port: 80
    targetPort: 80
    protocol: TCP
  type: ClusterIP